<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- res/values/styles.xml -->
    <style name="SpinnerTextWhite" parent="ThemeOverlay.AppCompat.Light">
        <!-- Spinner에서 사용하는 기본 텍스트 색 -->
        <item name="android:textColor">@android:color/white</item>
        <!-- 드롭다운 목록 항목에도 흰색 적용 -->
        <item name="android:popupTheme">@style/SpinnerDropDownTextWhite</item>
    </style>

    <style name="SpinnerDropDownTextWhite" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColor">@android:color/white</item>
    </style>
</resources>